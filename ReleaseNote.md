# Release Notes

## 2.0.0 (2026-08-25)

### Added
- **批注与评审系统**：支持选中文本批注、回复、状态流转、失效重定位、编辑面浅蓝高亮，以及 Markdown / HTML 导出。
- **Markdown WYSIWYG 编辑器**：新增所见即所得编辑体验，支持表格工具条、mermaid 图表渲染、frontmatter 元数据卡、编辑面 Ctrl+F 搜索。
- **会话持久化**：新增会话存储、备份服务、自动备份调度、最近文件与恢复栏。
- **单实例文档接管**：通过文件关联再次启动 Flux 时，由现有窗口接收文档，保留当前工作区并将外部文档加入工作集；设置页提供“设为默认应用”入口。
- **编辑器右键菜单**：新增 Markdown 右键菜单、AI 审阅入口与批量附件备份。
- **Markdown 本地图片编辑**：编辑与源码界面支持粘贴、拖入多张图片，自动复制到文档附件目录并写入标准相对路径；支持相对路径、Windows 绝对路径及本地 SVG 显示。
- **工作区实时刷新**：打开文件夹后监听文件变化，新建、删除和重命名文本文件会自动刷新文件树。
- **AI 批注上下文**：评审批注可按需注入 AI 对话，辅助分析并生成文件修改预览。
- **编辑快捷键**：新增 Ctrl+1 至 Ctrl+5 标题，以及粗体、斜体、链接、引用、代码块和列表快捷键。
- **Agent 文件工具边界**：读取、搜索和文件信息查询限制在当前工作区、已打开文件及显式附件路径；目录搜索增加文件数量、单文件大小和取消边界。
- **AI 写入确认链路**：`write_file` 先经过主进程路径校验，再登记差异预览；事务回滚时会删除本次新建的文件。
- **帮助与发布文档**：更新帮助文档、README、ReleaseNote 与测试覆盖。

### Changed
- **编辑器稳定性**：增强编辑器生命周期守卫，降低切换文件、关闭文件和模式切换时的异常风险。
- **批注跳转语义**：单击批注条目仅高亮，双击批注条目才跳转到正文位置。
- **大纲定位**：大纲跳过 frontmatter 与代码块内容，减少伪标题干扰。
- **编辑样式**：优化行内代码、代码块、水平分割线、批注高亮与匹配项对比度。
- **代码块高亮范围**：Markdown、MD、Text、TXT、Log 代码块保持纯文本显示，仅对 JavaScript、TypeScript、JSON、Python、Bash、SQL 等代码语言着色。
- **会话与文件处理**：优化上下文预算、文件处理、会话处理和工作区扫描。
- **工作区零污染**：取消在用户文件夹中自动创建 `config` 目录及配置文件。
- **Markdown 视图缩放**：mermaid 图表跟随编辑区缩放，并保持更清晰的矢量渲染。
- **报告交付**：报告类 Skill 恢复显示“导出报告”，普通分析回复不会误判为导出任务。
- **图标与打包**：更新品牌 logo、运行时任务栏图标、应用标识及额外资源配置；Windows 发布提供安装版与便携版。

### Fixed
- 修复编辑界面关闭文件、切换编辑模式时的崩溃问题。
- 修复编辑界面批注高亮装饰越界导致的编辑事务崩溃。
- 修复源码界面编辑时误触发批注跳转。
- 修复批注双击跳转失效。
- 修复 mermaid 代码块空块键入、粘贴时内容丢失。
- 修复列表项后 `> ` 引用块未按引用样式渲染。
- 修复大纲和帮助中心引用代码块内标题、frontmatter 幻影条目问题。
- 修复 CodeMirror 聚焦虚线描边闪现、批注选中文本棕色样式。
- 修复工作区新增文件不能及时识别的问题。
- 修复取消未保存提示后工作区监听被提前停止的问题。
- 修复 Ctrl+Shift+7、Ctrl+Shift+8、Ctrl+Shift+9 在部分键盘布局下失效的问题。
- 修复编辑视图缩放时 mermaid 图表尺寸不跟随的问题。
- 修复图片拖放同时触发编辑器插图和全局文件导入的问题。
- 修复文档刚加载时图片占位按钮继承临时只读状态、双击无法选择图片的问题。
- 修复 Windows 绝对路径被误判为 URL 协议，导致本地 SVG 无法显示的问题。
- 修复 Markdown 右键菜单与子菜单之间存在间隙、移动鼠标时子菜单提前关闭的问题。
- 修复编辑视图缩小到 100% 以下时正文偏移或左侧裁剪的问题。
- 修复 Markdown 编辑区与源码区切换时滚动位置回到顶部的问题，切换前同步保存并按比例恢复。
- 修复已有工作区时通过 Windows“打开方式”接收外部 Markdown 正文为空的问题。
- 修复 AI 写入预览仍读取模型原始参数、未使用主进程校验结果的问题。

## 1.0.2 (2026-06-04)

### Added
- **会话上下文管理**：对话区新增上下文占用条，支持「压缩上下文」「新建对话」；历史消息与 Tool 输出按预算自动裁剪，降低长对话内存与 token 压力。
- **钉住结论**：AI 回复可一键钉住关键结论，后续轮次与新建对话仍会注入 system；支持取消钉住。
- **工作区会话持久化**：压缩或钉住后自动写入 `{工作区}/.flux/session-summary.md`，重开工作区可恢复摘要与钉住项。
- **大日志索引视图**：超过 2MB 的 `.log` 走 LogViewer 按需读行，不再全量载入编辑器，显著降低峰值内存。
- **Markdown 本地图片**：预览模式下 `![](相对路径)` 可正确显示工作区内的本地图片（通过 `flux-local` 协议加载）。
- **Markdown 文档跳转**：预览中点击 `[链接](./other.md)` 等相对路径链接，会关闭当前文档并在工作区直接打开目标 Markdown 文件。

### Changed
- **报告导出**：Flux 主导分析报告导出；Agent 默认不再通过 `write_file` 写报告文件，导出正文优先取自工具输出中的实质 Markdown 内容。
- **AI 上下文组装**：统一 system / preface / history 字符预算（standard 128K 档位）；大文件（>512KB）不注入全文，大 `.log` 仅注入索引摘要（≤2KB）。
- **Tool 结果**：进入对话历史的 Tool 输出默认摘要化；`read_file` 默认分段读取，单次结果有上限 clamp。

### Fixed
- 修复 Markdown 预览中相对路径图片无法显示的问题。
- 修复 Markdown 预览中 `.md` 相对链接点击无效或导航错误的问题。
- 修复 `LogViewer` 组件与类型同名冲突，避免开发模式下界面白屏。

## 1.0.1 (2026-05-17)

### Added
- 内置 Skill（flux-core、problem-summary、analysis-report）打包入 asar
- 设置页”自定义”提供商新增”协议类型”下拉，可在 OpenAI Chat Completions 与 Anthropic Messages 之间切换。
- 协议下拉选项新增端点提示（`/v1/chat/completions`、`/v1/messages`），便于快速校验对接协议。

### Changed
- 导出分析报告成功后，自动刷新当前工作区文件列表，导出文件可立即在文件树中可见。
- 自定义协议类型会参与“测试连接”和“保存配置”，不再固定按单一协议发送请求。
- 设置页下拉触发器视觉优化：箭头样式与大小重新调整，提升识别度并减少突兀感。

### Fixed
- 修复导出报告后“已保存但工作区未刷新”的问题。
- 修复部分自定义配置在回显后协议设置项不易出现的问题（显示逻辑已放宽）。

## 1.0.0 (Released)

### Added
- 帮助菜单新增“关于”入口。
- 新增 About 弹窗：显示应用图标、当前版本与 website 链接。
- 新增 `app:get-version` IPC 通道，渲染进程可读取主进程版本号。
- 提供三栏工作台：文件管理、编辑器、AI 对话。
- 支持 Skill 导入与调用、Provider 配置、Markdown 报告导出。

### Changed
- AI 对话输入支持最多 5 条历史输入（方向键上下切换）。
- 历史切换时抑制 `/skill` 与 `@` 弹窗，避免卡住；重新输入后恢复弹出。
- 应用改为单实例模式：第二次启动弹窗提示“已在运行”，并自动退出。
- 新建文件默认名统一为 `untitled.md`。
- 新建时覆盖已有文件会先清空目标内容，避免残留旧文本。

### Fixed
- 左侧工作集支持关闭已打开文件（`×`）。
- 首个发布版本。
