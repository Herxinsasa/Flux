# baseline2.3 需求设计

## 批注线程

根批注代表评审意见；回复包含 `role`、正文和时间。默认回复身份为修改人，也允许评审人继续追问。旧批注缺少回复数组时按空数组处理。

## 表格工具

表格获得焦点时，在左上角显示固定四按钮工具条。行列按钮打开紧凑调整面板；对齐按钮作用于当前列。

## 工作区

顶栏增加工作区图标，与批注、AI 面板开关保持同一视觉和交互模式。状态复用现有持久化布局字段。

## 设置

保留单页并使用二级标题，避免为少量配置增加导航成本。供应商连接为主区；AI 会话数据属于高级设置并默认折叠。表单编辑不自动写磁盘，继续使用明确保存按钮。
