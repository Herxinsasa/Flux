# baseline2.2 UI 约束

- Markdown 源码选区不显示悬浮 AI 工具条，右键菜单是唯一上下文入口。
- 顶部“段落”菜单沿用“样式 / 段落 / 插入”分组和名称，不新增说明文字。
- 大纲保持左侧，点击后当前编辑面平滑定位，不切换模式。
- 未保存对话框仅保留文件名、简短风险提示和“保存 / 不保存 / 取消”三个动作。
- 恢复预览直接使用文档阅读样式，不出现源码框。
- 返回操作统一显示“返回”，不带左箭头。
- 设置页不出现“阅读与编辑”或 AI 绘画区。

