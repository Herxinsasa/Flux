# baseline2.1 编辑交互人工验收清单

> 使用方式：通过后勾选对应项；未通过时保持未勾选，并在“Bug 描述”后补充现象、文件和复现步骤。

## 1. 模式与布局

- [ ] `UI-001` 顶部仅显示“编辑 / 源码 / 大纲”，没有“分栏”，“即时编辑”已改为“编辑”。
  - Bug 描述：
- [ ] `UI-002` 点击大纲后，大纲出现在正文左侧；关闭后正文恢复宽度。
  - Bug 描述：
- [ ] `UI-003` 切换文件后大纲自动关闭，文件内容和当前模式不丢失。
  - Bug 描述：
- [ ] `UI-004` 在窄窗口打开大纲，工具栏可换行且正文、按钮、文件名不重叠。
  - Bug 描述：

## 2. 内容缩放

- [ ] `ZOOM-001` 编辑模式点击放大/缩小，每次变化一级，比例数值同步变化。
  - Bug 描述：
- [ ] `ZOOM-002` 源码模式点击放大/缩小，每次只变化一级，不会跳两级。
  - Bug 描述：
- [ ] `ZOOM-003` `Ctrl+=`、`Ctrl+-`、`Ctrl+0` 在两种模式下分别放大、缩小、复位。
  - Bug 描述：
- [ ] `ZOOM-004` 缩放只影响 Markdown 内容，不放大工具栏、大纲和应用侧栏。
  - Bug 描述：
- [ ] `ZOOM-005` 放大至上限后正文仍可滚动，表格和代码块不越界遮挡。
  - Bug 描述：

## 3. 源码右键菜单

- [ ] `SRC-001` 无选区右键仍显示完整菜单；引用、批注和选择依赖样式置灰。
  - Bug 描述：
- [ ] `SRC-002` 选中文本后，粗体、斜体、行内代码生成正确 Markdown 并保持可保存。
  - Bug 描述：
- [ ] `SRC-003` 引用块、有序列表、无序列表、任务列表能转换多行选区。
  - Bug 描述：
- [ ] `SRC-004` 无选区时一级至五级标题作用于右键所在段落。
  - Bug 描述：
- [ ] `SRC-005` 链接、图片、表格、代码块、分割线插入在右键光标位置。
  - Bug 描述：
- [ ] `SRC-006` 右键菜单靠近窗口右下角时不会超出屏幕，子菜单可正常展开。
  - Bug 描述：

## 4. 编辑右键菜单

- [ ] `EDIT-001` 编辑模式无选区右键显示相同菜单结构，选择依赖项置灰。
  - Bug 描述：
- [ ] `EDIT-002` 粗体、斜体、行内代码、引用和列表操作即时渲染，切到源码后 Markdown 正确。
  - Bug 描述：
- [ ] `EDIT-003` 一级至五级标题作用于当前段落，空段落也可设置标题。
  - Bug 描述：
- [ ] `EDIT-004` 插入链接、图片、3x3 表格、代码块、分割线后可继续输入。
  - Bug 描述：
- [ ] `EDIT-005` 右键操作后内容自动进入未保存状态，`Ctrl+S` 保存并重新打开一致。
  - Bug 描述：

## 5. AI 引用与批注

- [ ] `AI-001` 未配置 AI 时“引用”置灰；其他本地编辑操作正常。
  - Bug 描述：
- [ ] `AI-002` 已配置 AI 时选中文字点“引用”，聊天栏自动展开并出现引用卡片。
  - Bug 描述：
- [ ] `REV-001` 源码选区添加批注后侧车批注出现，原 Markdown 不被写入批注文字。
  - Bug 描述：
- [ ] `REV-002` 编辑模式中唯一文本选区可以添加批注；重复文本无法可靠定位时批注置灰。
  - Bug 描述：

## 6. 表格编辑

- [ ] `TABLE-001` 新建或打开 GFM 表格后，鼠标进入表格会出现行列控制柄。
  - Bug 描述：
- [ ] `TABLE-002` 可选择当前行/列，并可在前后增加行列。
  - Bug 描述：
- [ ] `TABLE-003` 可删除选中行/列，删除后光标和表格结构仍有效。
  - Bug 描述：
- [ ] `TABLE-004` 列可设置左对齐、居中、右对齐，切到源码后分隔行标记正确。
  - Bug 描述：
- [ ] `TABLE-005` 行列拖动后内容顺序正确，保存并重新打开保持一致。
  - Bug 描述：
- [ ] `TABLE-006` 表格之外不显示控制柄；控制柄和菜单不遮挡单元格正文。
  - Bug 描述：

## 7. 回归

- [ ] `REG-001` 从外部粘贴包含标题、列表、任务、表格、代码块的 Markdown 能完整解析。
  - Bug 描述：
- [ ] `REG-002` 编辑、源码来回切换 10 次，内容不丢失、不重复、不出现空白区。
  - Bug 描述：
- [ ] `REG-003` 打开大文件后输入、滚动、右键和切换模式无明显持续卡顿。
  - Bug 描述：
- [ ] `REG-004` TXT/LOG/JSON 编辑或查看行为不受 Markdown 右键菜单和缩放快捷键影响。
  - Bug 描述：
