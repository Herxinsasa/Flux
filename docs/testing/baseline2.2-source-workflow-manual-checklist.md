# Flux baseline2.2 人工验收清单

## 填写规则

- 通过：把 `[ ]` 改成 `[x]`。
- 未通过：保持 `[ ]`，在该项下方的“Bug 描述”填写现象、操作步骤和涉及文件。
- 本轮仍处于 Bug 修复阶段，不执行打包。

## A. 源码长文本与基础编辑

- [ ] A-01 打开普通 `.md`，切换“源码”，正文完整显示，没有空白截断。
  Bug 描述：

- [ ] A-02 在源码中连续输入、回车、删除 30 秒，输入不会成批延迟或回滚。
  Bug 描述：

- [ ] A-03 打开接近 1-2 MB 的 Markdown/TXT，快速滚动和选择文本时没有明显卡死。
  Bug 描述：

- [ ] A-04 源码选中文本后不再自动弹出 AI 浮动工具条。
  Bug 描述：

- [ ] A-05 源码选中文字后右键，显示“引用 / 批注 / 样式 / 段落 / 插入”菜单。
  Bug 描述：

- [ ] A-06 源码未选中文字时，依赖选区的命令置灰；标题和插入命令仍可使用。
  Bug 描述：

- [ ] A-07 源码修改后按 `Ctrl+S`，关闭并重新打开文件，内容与保存结果一致。
  Bug 描述：

## B. 编辑模式与批注

- [ ] B-01 “编辑”模式可直接修改正文，标题、列表、引用、代码块和表格保持即时渲染。
  Bug 描述：

- [ ] B-02 编辑模式选中普通文本，右键“批注”可打开批注输入框并保存。
  Bug 描述：

- [ ] B-03 编辑模式选中粗体、斜体、链接文字或行内代码，右键“批注”仍可用。
  Bug 描述：

- [ ] B-04 文档存在相同文字两处时，给第二处添加批注，侧栏和高亮定位到第二处。
  Bug 描述：

- [ ] B-05 批注保存后原 `.md` 正文不包含批注文本，侧车 `.review.json` 正常更新。
  Bug 描述：

## C. 顶部菜单与右键命令一致性

- [ ] C-01 顶部“段落”菜单按“样式 / 段落 / 插入”分组，名称与右键菜单一致。
  Bug 描述：

- [ ] C-02 在源码模式使用顶部粗体、斜体、代码、引用、三类列表，结果正确。
  Bug 描述：

- [ ] C-03 在编辑模式使用顶部粗体、斜体、代码、引用、三类列表，结果正确且不切换界面。
  Bug 描述：

- [ ] C-04 在源码和编辑模式分别测试一级至五级标题，命令作用于当前段落。
  Bug 描述：

- [ ] C-05 在源码和编辑模式分别插入链接、图片、表格、代码块、分割线。
  Bug 描述：

- [ ] C-06 表格插入后可使用现有行列增加、删除和列对齐控件。
  Bug 描述：

## D. 双模式大纲

- [ ] D-01 源码模式点击大纲标题，在源码中跳到对应行，不切换到编辑模式。
  Bug 描述：

- [ ] D-02 编辑模式点击大纲标题，在渲染编辑器中跳到对应标题，不切换到源码。
  Bug 描述：

- [ ] D-03 文档存在两个同名同级标题时，大纲两项分别跳到第一处和第二处。
  Bug 描述：

- [ ] D-04 长文连续输入时大纲稍后更新，但输入和滚动不被大纲解析阻塞。
  Bug 描述：

## E. 未保存切换保护

- [ ] E-01 修改当前文件后点击另一个已打开标签，出现“保存 / 不保存 / 取消”。
  Bug 描述：

- [ ] E-02 选择“取消”，当前文件、内容和未保存标记均保持不变。
  Bug 描述：

- [ ] E-03 选择“保存”，保存成功后切换到目标文件；重新打开来源文件内容已保存。
  Bug 描述：

- [ ] E-04 选择“不保存”，切换后重新打开来源文件，显示磁盘旧内容而不是被放弃的草稿。
  Bug 描述：

- [ ] E-05 分别通过工作区文件树、`Ctrl+Tab`、Markdown 链接和系统双击打开另一文件，均触发相同保护。
  Bug 描述：

- [ ] E-06 模拟外部修改造成版本冲突后选择“保存”，提示冲突且不会切换文件。
  Bug 描述：

## F. 关闭软件保护

- [ ] F-01 修改文件后点击窗口关闭，出现同一个三选一保存对话框。
  Bug 描述：

- [ ] F-02 关闭提示选择“取消”，软件保持打开且正文不变。
  Bug 描述：

- [ ] F-03 关闭提示选择“保存”，按钮显示保存中，保存成功后软件关闭。
  Bug 描述：

- [ ] F-04 关闭提示选择“不保存”，软件关闭；再次启动后原文件保持磁盘旧内容。
  Bug 描述：

## G. 恢复与界面剪枝

- [ ] G-01 有可恢复备份时点击“预览”，内容按 Markdown 阅读样式渲染，不显示源码块。
  Bug 描述：

- [ ] G-02 恢复预览中的相对图片和链接以原文件路径为基准正常解析。
  Bug 描述：

- [ ] G-03 设置、技能和帮助页面的返回按钮只显示“返回”，没有左箭头。
  Bug 描述：

- [ ] G-04 设置页不再显示“阅读与编辑”区域，内容缩放按钮和快捷键仍可使用。
  Bug 描述：

- [ ] G-05 设置页不存在 AI 绘画设置或无效占位入口。
  Bug 描述：

## H. 启动与回归

- [ ] H-01 `npm run dev` 可启动 Electron，无 `dialog module can only be used after app is ready` 错误。
  Bug 描述：

- [ ] H-02 重复启动 Flux 时提示已有实例，不影响原窗口继续使用。
  Bug 描述：

- [ ] H-03 打开文件夹、最近文件、自动备份、AI 对话和批注侧栏的既有功能未回归。
  Bug 描述：
