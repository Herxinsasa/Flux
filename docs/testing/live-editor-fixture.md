# Flux 即时编辑完整性测试

开头段落用于确认文档头部显示。

## 常规格式

- 列表一
- 列表二
- [ ] 待办事项
- [x] 已完成事项

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| 即时编辑 | 测试中 | 表格必须完整显示 |
| 保存 | 测试中 | 修改后写回 Markdown |

> 这是一段引用。

```typescript
export const flux = 'markdown-first'
```

<details>
<summary>原始 HTML</summary>

HTML 内容也必须保留。

</details>

## 长文档中段

中段 01。用于确认滚动区域没有被内部容器截断。

中段 02。用于确认滚动区域没有被内部容器截断。

中段 03。用于确认滚动区域没有被内部容器截断。

中段 04。用于确认滚动区域没有被内部容器截断。

中段 05。用于确认滚动区域没有被内部容器截断。

中段 06。用于确认滚动区域没有被内部容器截断。

中段 07。用于确认滚动区域没有被内部容器截断。

中段 08。用于确认滚动区域没有被内部容器截断。

中段 09。用于确认滚动区域没有被内部容器截断。

中段 10。用于确认滚动区域没有被内部容器截断。

中段 11。用于确认滚动区域没有被内部容器截断。

中段 12。用于确认滚动区域没有被内部容器截断。

中段 13。用于确认滚动区域没有被内部容器截断。

中段 14。用于确认滚动区域没有被内部容器截断。

中段 15。用于确认滚动区域没有被内部容器截断。

## 文档尾部

TAIL_SENTINEL_即时编辑必须显示到这里。
